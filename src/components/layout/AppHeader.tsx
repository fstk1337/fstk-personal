import AppNavbar from '../AppNavbar';
import { Box, CssBaseline } from '../../mui';

function AppHeader() {
    return (
        <Box>
            <CssBaseline />
            <AppNavbar />
        </Box>
    );
}

export default AppHeader;
